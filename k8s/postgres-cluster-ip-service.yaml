apiVersion: v1
kind: Service
metadata:
  name: postgres-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: postgre
  ports:
    - port: 5432 # for outside objects
      targetPort: 5432
